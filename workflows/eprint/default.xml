<?xml version="1.0" encoding="utf-8"?>
   <!--
   
   Action buttons (Previous/Save/Next) can be shown "top", "bottom" or "both":
   <stage name="type" action_buttons="both">
     <component><field ref="type" required="yes" /></component>
   </stage>
   
   -->
   
<workflow xmlns="http://eprints.org/ep3/workflow" xmlns:epc="http://eprints.org/ep3/control"
	<flow>
		<stage ref="funding"/>  		  		
	</flow>
	<stage name="funding">      
   		<component>
   			<field ref="funding" input_lookup_url="{$config{rel_cgipath}}/users/lookup/file" input_lookup_params="file=rs_enlighten.dat"/>
   		</component>
   	</stage> 
</workflow>
